package com.example.cats_mice_game


import android.graphics.Point
import kotlin.random.Random

fun main(){
    val currentPoint = Pair(0, 0)
    var destinationPoint = Pair(0, 0)

    println(currentPoint != destinationPoint)
}